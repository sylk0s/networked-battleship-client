package cs3500.pa04.json;

/**
 * Represents an orientation the battleship can be in
 */
public enum Orientation {
  /**
   * A ship extending downward from it's origin coord
   */
  VERTICAL,
  /**
   * A ship extending right from it's origin coord
   */
  HORIZONTAL,
}
