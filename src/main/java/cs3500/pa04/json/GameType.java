package cs3500.pa04.json;

/**
 * The type of game to join
 */
public enum GameType {
  /**
   * Play against the AI on the server
   */
  SINGLE,
  /**
   * Play against another player on the server
   */
  MULTI,
}
